(ns axrs.re-frame.http-fxs-test
  (:require
    [cljs.test :refer-macros [is are deftest testing use-fixtures]]
    [axrs.re-frame.http-fxs]))

(deftest testing-1-1
  (testing "Testing that 1+1 = 2"
    (is (= 1 2))))
